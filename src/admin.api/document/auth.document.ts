export const authApiOperationDescription = {
  createGoolgeUser:
    'redirect frontDomain + auth/google/callback?accessToken=token',

  createTwitchUser: `redirect frontDomain + auth/twitch/callback?accessToken=token`,

  deleteUser: `유저 탈퇴`,
};
